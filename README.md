# geo3-d

Experimental local skeleton for coordinate-system-agnostic 3D Euclidean
geometry.

The current repository exists only to validate the planned API relationship
between `geo-d`, `geo3-d`, and their internal shared-contract layer.

The public consumer facade is `import geo3;`.

Consumers should not need to import the shared core directly.
